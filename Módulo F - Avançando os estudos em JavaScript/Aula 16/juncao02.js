function soma(n1=0, n2=0) {//caso a função soma não receba um parametro, ele por padrão valerá 0, evitando que retorne NaN
    return n1 + n2 //retorna a soma de n1 + n2
}

console.log(soma(8, 5)) //envia os parametos para a função e exibe o retorno no console